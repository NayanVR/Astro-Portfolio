<nav data-open="false" class="fixed top-8 left-8 z-[100] group">
    <button class="w-20 h-20 relative bg-light rounded-full flex items-center justify-center">
        <div class="w-24 h-24 absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 border-2 border-dashed border-light rounded-full animate-spin"></div>
        <img class="w-1/2 h-1/2" src="/black-logo.svg" alt="NayanVR Logo">
    </button> 
    <div class="w-[1000px] h-[1000px] scale-[5] ease-in-out group-data-[open=false]:scale-0 transition-all duration-1000  bg-dark absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 -z-10 rounded-full">
    </div>
    <div class="w-screen h-screen group-data-[open=false]:scale-0 flex flex-col justify-center items-center absolute -top-8 -left-8 -z-10 transition-all duration-1000 origin-top-left">
        <h1>Hellooooooooo</h1>
    </div>
</nav>

<script>
    const navBtn: HTMLElement = document.querySelector('nav button');
    const nav: HTMLElement = document.querySelector('nav');

    navBtn.addEventListener('click', () => {
        nav.dataset.open = nav.dataset.open === 'true' ? 'false' : 'true';
    });
</script>